<% @publications.each do |publication| %>
  <% present publication, PublicationPresenter do |p| %>
    <%= p.title %>
    <%= p.excerpt %>

    Published at: <%= p.published %>
    Published as: <%= p.type %> 
    Authored by: <%= p.author %>

  <% end %>
<% end %>
